import { strict as assert } from "node:assert"
import { Machine, Response } from "../src/machine"

describe("machine", function () {
  describe("Machine", function () {
    it("generates responds to incoming messages")
  })
})
